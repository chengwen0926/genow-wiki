[project]
name = "genow-wiki-backend"
version = "0.1.0"
description = "FastAPI backend for the Genow Wiki app"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.3",
    "uvicorn>=0.49.0",
]

[tool.uv]
package = false
